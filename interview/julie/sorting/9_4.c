#include <stdio.h>
#include <string.h>

//9.4
/*
Question: If you have a 2GB file with one string per line, which sorting algorithm would you use to sort the file and why?
*/

/*Analysis: No test needed for this question
  Considering the time complexity, mergeSort is the best solution(Onlog(n))
  But mergeSort requires the extra space for doing the merge
*/
